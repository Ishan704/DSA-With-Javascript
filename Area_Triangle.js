const prompt=require('prompt-sync')()
let Base=parseInt(prompt("Enter Base of Triangle"));
let Height=parseInt(prompt("Enter Base of Triangle"));
let ans=.5*Base*Height;
console.log("Area of triangle="+ans);